# 注意事项

## 代码提交规范
- 每次改动完成后，都必须创建一个对应的 Git commit，以便后续追踪和回滚。
- **Commit message 格式**：必须遵循 `<type>(<scope>): <subject>` 格式，例如 `feat(auth): add login validation`。常用 type 包括：feat、fix、docs、style、refactor、test、chore。
- **提交粒度**：每个 commit 只包含一个逻辑上独立的改动，禁止把多个不相关的改动混在一起提交。

## 测试规范
- 每次改动后，都必须编写或更新相关测试，并在交付给用户前，确保所有测试和验证全部通过。
- **测试范围**：至少包含核心功能的单元测试，以及对改动涉及路径的回归测试。
- **测试通过标准**：所有测试用例必须全部通过（Green），不允许有跳过的测试（skipped）或未执行的用例。

## 交付前检查清单（必须逐项确认）
- [ ] 所有代码改动已提交到 Git，且 commit message 清晰规范
- [ ] 测试套件全部通过（绿色）
- [ ] 无新增的 linter 错误或警告
- [ ] 改动涉及的功能已在本地手动验证过
- [ ] 无明显的影响其他模块的副作用
