{
  "name": "@app/extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Chrome/Edge MV3 extension that captures audio from any video webpage and overlays real-time bilingual subtitles (talks to the local @app/server backend).",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.3"
  }
}
